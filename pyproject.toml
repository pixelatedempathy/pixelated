[project]
name = "pixelated"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "black>=25.1.0",
    "mypy>=1.18.1",
    "pyyaml>=6.0.2",
    "ruff>=0.12.12",
]
