{
  "version": 2,
  "installCommand": "corepack enable && corepack prepare pnpm@10.26.0 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && corepack prepare pnpm@10.26.0 --activate && pnpm build",
  "builds": [
    {
      "src": "deploy/vercel/vercel-handler.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "dist/**"
        ]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/deploy/vercel/vercel-handler.js"
    }
  ]
}