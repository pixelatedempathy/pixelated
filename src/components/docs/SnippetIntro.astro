---
// SnippetIntro component for introducing reusable snippets
export interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<div class={`snippet-intro ${className || ''}`}>
  <div class="intro-content">
    <h2>Reusable Snippets</h2>
    <p>
      Snippets are reusable pieces of content that can be included in multiple
      documentation pages. They help maintain consistency and reduce duplication
      across your documentation.
    </p>
    <div class="features-grid">
      <div class="feature">
        <h3>🔄 Reusable</h3>
        <p>
          Write once, use everywhere. Update in one place to reflect changes
          across all pages.
        </p>
      </div>
      <div class="feature">
        <h3>📝 Markdown Support</h3>
        <p>
          Full Markdown and MDX support with all the components you're used to.
        </p>
      </div>
      <div class="feature">
        <h3>🎯 Targeted</h3>
        <p>
          Include specific snippets where needed without cluttering your main
          content.
        </p>
      </div>
    </div>
  </div>
</div>

<style>
  .snippet-intro {
    @apply mb-8 p-6 bg-gradient-to-r from-blue-50 to-indigo-50 border border-blue-200 rounded-lg dark:from-blue-900/20 dark:to-indigo-900/20 dark:border-blue-800;
  }

  .intro-content h2 {
    @apply text-2xl font-bold text-blue-900 dark:text-blue-100 mb-4;
  }

  .intro-content > p {
    @apply text-blue-800 dark:text-blue-200 mb-6 text-lg;
  }

  .features-grid {
    @apply grid grid-cols-1 md:grid-cols-3 gap-4;
  }

  .feature {
    @apply bg-white dark:bg-gray-800 p-4 rounded-lg border border-blue-100 dark:border-blue-700;
  }

  .feature h3 {
    @apply text-lg font-semibold text-blue-900 dark:text-blue-100 mb-2;
  }

  .feature p {
    @apply text-blue-700 dark:text-blue-300 text-sm;
  }
</style>
