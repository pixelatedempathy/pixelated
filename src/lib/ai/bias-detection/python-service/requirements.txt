# Bias Detection Engine Python Service Requirements
# Core fairness and bias detection libraries
aif360
fairlearn
witwidget
evaluate

# Flask web framework and extensions
Flask
Flask-CORS
Werkzeug>=3.1.4  # Fixes GHSA-hgf8-39gv-g3f2 (Windows special device names vulnerability)

# Authentication and security
PyJWT
cryptography

# NLP processing libraries for linguistic bias detection
spacy
nltk
textblob
transformers
torch>=2.8.0
datasets

# Data processing and ML
pandas
numpy
scikit-learn>=1.5.0
scipy

# Visualization and monitoring
matplotlib
seaborn
plotly

# Model explanation and interpretability
shap
lime

# Data validation and quality
great-expectations
pandera

# Additional utilities
tqdm
jsonlines
pyyaml
requests>=2.32.4  # Fixes CVE-2024-47081 (.netrc credentials leak via malicious URLs)

# Development and testing
pytest
pytest-mock
pytest-flask
black
flake8

# Production server
gunicorn
gevent

# Distributed task processing
celery[redis]
redis

# Demographic analysis (optional)
face-recognition

# Environment and configuration
python-dotenv

# Security: Pin transitive dependencies to secure versions
werkzeug>=3.1.4  # Fixes GHSA-hgf8-39gv-g3f2 (Windows special device names vulnerability)
fonttools>=4.61.0  # Fixes GHSA-768j-98cg-p3fv (Arbitrary File Write and XML injection vulnerability)
tensorflow>=2.12.0 # not directly required, pinned by Snyk to avoid a vulnerability
protobuf>=4.25.8 # not directly required, pinned by Snyk to avoid a vulnerability
filelock>=3.20.1

# Additional required dependencies to resolve warnings
scipy>=1.9.0
pyzmq>=24.0.0
fonttools>=4.61.0
scikit-image>=0.19.0
kombu>=5.3.0
safetensors>=0.3.0
urllib3>=2.6.2  # Fixes GHSA-gm62-xv2j-4w53, GHSA-2xpw-w6gg-jr37 (CVE-2025-66418, CVE-2025-66471)
filelock>=3.20.1  # not directly required; pinned by Snyk to avoid SNYK-PYTHON-FILELOCK-14458335
