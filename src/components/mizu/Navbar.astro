---
const links = [
  { text: 'Home', href: '#' },
  { text: 'Features', href: '#features' },
  { text: 'Testimonials', href: '#testimonials' },
  { text: 'FAQ', href: '#faq' },
]
---

<header class="fixed top-0 w-full z-50 bg-slate-950/80 backdrop-blur-md border-b border-white/10">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-20">
      <div class="flex-shrink-0 flex items-center gap-2">
        <!-- Logo Icon -->
        <div class="w-8 h-8 bg-gradient-to-br from-primary-400 to-primary-600 rounded-lg flex items-center justify-center text-white font-bold shadow-lg shadow-primary-500/20">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z" fill="currentColor"/>
          </svg>
        </div>
        <span class="font-bold text-xl tracking-tight text-white">Pixelated Empathy</span>
      </div>

      <nav class="hidden md:flex space-x-8">
        {links.map((link) => (
          <a href={link.href} class="text-slate-300 hover:text-white transition-colors text-sm font-medium">
            {link.text}
          </a>
        ))}
      </nav>

      <div class="hidden md:flex items-center gap-4">
        <a href="/login" class="text-slate-300 hover:text-white text-sm font-medium transition-colors">
          Log in
        </a>
        <a href="/auth/signup" class="bg-white text-slate-950 px-5 py-2.5 rounded-full text-sm font-semibold hover:bg-slate-200 transition-colors">
          Get Started
        </a>
      </div>
    </div>
  </div>
</header>
