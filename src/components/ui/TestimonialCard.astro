---
interface Props {
  quote: string
  author: string
  title: string
  institution?: string
  photo?: string
  rating?: number
}

const { quote, author, title, institution, photo, rating } = Astro.props
---

<div class="bg-black/40 backdrop-blur-xl border border-white/10 rounded-2xl p-8 hover:border-white/20 transition-all duration-300 h-full flex flex-col">
  {rating && (
    <div class="flex gap-1 mb-4">
      {Array.from({ length: 5 }).map((_, i) => (
        <svg class={`w-5 h-5 ${i < rating ? 'text-yellow-400' : 'text-gray-600'}`} fill="currentColor" viewBox="0 0 20 20">
          <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
        </svg>
      ))}
    </div>
  )}
  
  <blockquote class="text-gray-300 leading-relaxed mb-6 flex-1">
    "{quote}"
  </blockquote>
  
  <div class="flex items-center gap-4">
    {photo ? (
      <img src={photo} alt={author} class="w-12 h-12 rounded-full object-cover" />
    ) : (
      <div class="w-12 h-12 rounded-full bg-gradient-to-br from-emerald-500 to-cyan-500 flex items-center justify-center">
        <span class="text-white font-bold text-lg">{author.charAt(0)}</span>
      </div>
    )}
    <div>
      <div class="font-semibold text-white">{author}</div>
      <div class="text-sm text-gray-400">{title}</div>
      {institution && (
        <div class="text-xs text-gray-500">{institution}</div>
      )}
    </div>
  </div>
</div>
