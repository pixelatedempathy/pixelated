---
interface Props {
  as?: string
  elevated?: boolean
  class?: string
}

const { as = 'div', elevated = false, class: className = '' } = Astro.props
const Tag = as as any
---

<Tag
  class={`surface${elevated ? ' surface--elevated' : ''} ${className}`.trim()}
>
  <slot />
</Tag>
