---
import BaseLayout from '@/layouts/BaseLayout.astro'
import CardView from '@/components/views/CardView.astro'

const frontmatter = {
  title: 'Highlights',
  subtitle: 'Showcase your creative work or curated posts',
  description: 'Showcase your creative work or curated posts',
  bgType: 'default',
  toc: false,
  ogImage: '/og-image.png',
}
---

<BaseLayout
  title={frontmatter.title}
  description={frontmatter.description}
  bgType={frontmatter.bgType}
>
  <section class="py-12">
    <div class="max-w-6xl mx-auto px-4 text-center">
      <h1 class="text-4xl font-bold text-white mb-4">{frontmatter.title}</h1>
      <p class="text-lg text-slate-400 mb-12">{frontmatter.subtitle}</p>
      <CardView collectionType="highlights" />
    </div>
  </section>
</BaseLayout>
