// Deployment configuration for Pixelated Empathy
// Add deployment-specific settings here

const deploymentConfig = {
  region: 'us-east-1',
  staticAssets: true,
  edge: false,
}

export default deploymentConfig
