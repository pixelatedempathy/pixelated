version: '3.8'

services:
  # Define your services here (e.g., web, db, etc.)
  # The original file likely contained additional service definitions;
  # keep the structure intact for Docker Compose to work.

const headless = process.env.PLAYWRIGHT_HEADLESS !== 'false';

export default defineConfig({
  testDir: 'tests',