---
const features = [
  {
    icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/>`,
    title: 'AI-Powered Scenarios',
    description:
      'Experience realistic patient interactions through advanced AI simulation that adapts to your responses in real-time.',
    stats: '10,000+ scenarios',
    color: 'var(--accent-primary)',
  },
  {
    icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>`,
    title: 'Real-Time Analytics',
    description:
      'Track your progress with detailed analytics on empathy scores, response quality, and therapeutic effectiveness.',
    stats: '95% accuracy',
    color: 'var(--accent-blue)',
  },
  {
    icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.746 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/>`,
    title: 'Evidence-Based Training',
    description:
      'Built on proven therapeutic methodologies and continuously updated with the latest research findings.',
    stats: '50+ frameworks',
    color: 'var(--accent-purple)',
  },
  {
    icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/>`,
    title: 'Collaborative Learning',
    description:
      'Share insights with peers, participate in group sessions, and learn from expert-led discussions.',
    stats: '24/7 community',
    color: 'var(--accent-orange)',
  },
  {
    icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/>`,
    title: 'HIPAA Compliant',
    description:
      'Enterprise-grade security ensuring all training data and interactions meet healthcare privacy standards.',
    stats: '100% secure',
    color: 'var(--accent-red)',
  },
  {
    icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>`,
    title: 'Instant Feedback',
    description:
      'Receive immediate, actionable feedback on your therapeutic responses and communication style.',
    stats: '< 100ms response',
    color: 'var(--accent-primary)',
  },
]
---

<section class="features-section">
  <div class="container-refined features-container">
    <!-- Section Header -->
    <div class="features-header">
      <div class="section-label">Features</div>
      
      <div class="features-badge">
        <svg width="16" height="16" fill="currentColor" viewBox="0 0 24 24">
          <path
            d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"
          ></path>
        </svg>
        <span>PLATFORM FEATURES</span>
      </div>

      <h2 class="features-title">Advanced AI Training</h2>

      <p class="features-description">
        Cutting-edge technology meets evidence-based therapeutic training in our
        comprehensive platform designed for mental health professionals.
      </p>
    </div>

    <!-- Features Grid -->
    <div class="features-grid">
      {
        features.map((feature) => (
          <div class="feature-card">
            <div class="feature-icon" style={`background: ${feature.color};`}>
              <svg
                width="24"
                height="24"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
                style="color: white;"
              >
                <path set:html={feature.icon} />
              </svg>
            </div>

            <div class="feature-content">
              <div class="feature-header">
                <h3 class="feature-title">{feature.title}</h3>
                <span class="feature-badge" style={`color: ${feature.color};`}>
                  {feature.stats}
                </span>
              </div>

              <p class="feature-text">{feature.description}</p>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>

<style>
  /* Mizu-inspired Clean Features Section (More spacious) */
  .features-section {
    padding: var(--space-40) 0;
    background: var(--bg-primary);
    position: relative;
    overflow: hidden;
  }

  .features-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: var(--gradient-section);
    opacity: 0.2;
    pointer-events: none;
  }

  .features-container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 var(--space-8);
    position: relative;
    z-index: 1;
  }

  .features-header {
    text-align: center;
    margin-bottom: var(--space-20);
  }

  .section-label {
    font-size: var(--text-xs);
    font-weight: var(--weight-semibold);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--accent-primary);
    margin-bottom: var(--space-6);
    display: block;
  }

  .features-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-3) var(--space-6);
    background: rgba(16, 185, 129, 0.08);
    border: 1px solid rgba(16, 185, 129, 0.15);
    border-radius: var(--radius-full);
    color: var(--accent-primary);
    font-size: var(--text-sm);
    font-weight: var(--weight-semibold);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    margin-bottom: var(--space-8);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }

  .features-title {
    font-size: clamp(2.25rem, 5vw, 3.5rem);
    font-weight: var(--weight-black);
    line-height: 1.15;
    letter-spacing: -0.02em;
    color: var(--text-primary);
    margin: 0 0 var(--space-8) 0;
  }

  .features-description {
    font-size: var(--text-xl);
    color: var(--text-secondary);
    max-width: 700px;
    margin: 0 auto;
    line-height: 1.75;
    letter-spacing: 0.01em;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
    gap: var(--space-8);
  }

  @media (max-width: 768px) {
    .features-grid {
      grid-template-columns: 1fr;
    }
  }

  .feature-card {
    background: var(--bg-card);
    border: 1px solid var(--border-primary);
    border-radius: var(--radius-2xl);
    padding: var(--space-8);
    transition: all var(--transition-base);
    position: relative;
    overflow: hidden;
  }

  .feature-card:hover {
    border-color: var(--border-secondary);
    transform: translateY(-2px);
    box-shadow: var(--shadow-card);
  }

  .feature-icon {
    width: 5rem;
    height: 5rem;
    border-radius: var(--radius-xl);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: var(--space-8);
    position: relative;
    z-index: 1;
    box-shadow: var(--shadow-sm);
  }

  .feature-icon svg {
    width: 2rem;
    height: 2rem;
  }

  .feature-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    position: relative;
    z-index: 1;
  }

  .feature-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: var(--space-6);
  }

  .feature-title {
    font-size: var(--text-xl);
    font-weight: var(--weight-bold);
    color: var(--text-primary);
    margin: 0;
    flex: 1;
    letter-spacing: -0.01em;
  }

  .feature-badge {
    padding: var(--space-2) var(--space-4);
    background: rgba(16, 185, 129, 0.08);
    border: 1px solid rgba(16, 185, 129, 0.15);
    border-radius: var(--radius-md);
    font-size: var(--text-xs);
    font-weight: var(--weight-medium);
    white-space: nowrap;
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
  }

  .feature-text {
    font-size: var(--text-base);
    color: var(--text-secondary);
    line-height: 1.75;
    margin: 0;
    letter-spacing: 0.01em;
  }
</style>
