# Skip postinstall scripts that require native compilation
# This prevents timeouts on Cloudflare Pages builds
ignore-scripts=true

# Don't fail on peer dependency warnings
strict-peer-dependencies=false

# Use hoisted node_modules for better compatibility
shamefully-hoist=true
