# 🤝 AGENT HANDOFF: THE SYSTEMIC DEFENSE INTEGRATION

> **IMPORTANT FOR INCOMING AGENT:**
>
> Read this document first to instantly acquire the mental context of where
> the project currently stands. Do not reinvent the wheel. Read `AGENTS.md`
> before writing a single line of code.

## 🧭 The Current Mission

We are taking the baseline **PsyDefDetect** (Defense Mechanism Classifier via
DeBERTa) and weaving it deeply into the Pixelated Empathy platform
architecture. The goal is to move from a static inference model to a
real-time component of the Empathy Gym's feedback loop.

## 📍 Where We Are Right Now (State)

- **Gestalt Fusion Engine (PIX-149):** COMPLETED. The core engine is live in
  `ai/core/gestalt_engine.py` with a comprehensive 47-test suite.
- **WebSocket Integration (PIX-147):** IN PROGRESS. The `GestaltEngine` has
  been wired into `TrainingWebSocketServer.ts` via `GestaltClient.ts`. Real-time
  `gestalt_update` events are now broadcasted for Seeker messages.
- **Synthetic Data Generation Loop:** Currently running silently in the
  background (`ai/training/defense_mechanisms/generate_synthetic_loop.py`).
- **Architecture Spec:** Full specification in
  `docs/epics/systemic-defense-integration.md`.
- **AGENTS.md:** centralized rules at `.agent/`.

## 🎯 Next Immediate Action

## **PIX-148: Adversarial Persona Injection (The Dynamic Patient)**

The next agent should implement the closed-loop control system for adversarial
injection.

- **Goal:** Use the `persona_directive` from the `GestaltState` and inject it
  into the generative LLM's system prompt.
- **Why:** To ensure the AI patient remains stubborn and defensive when appropriate,
  preventing "cooperative compliance" during tension-heavy sparring.
- **Where to build:** Integrate with the chat/inference orchestration logic
  (likely in `src/pages/api/ai/pixel/infer.ts` or the Python pipeline).

## 🛑 Strict Rules for Incoming Agent

1. **NO STUBS:** Write real, production-ready code.
2. **UTILIZE DIRECTIVES:** Use `GestaltState.persona_directive` generated by the
   engine.
3. **CLOSED LOOP:** Ensure the injection happens _before_ the generator starts,
   creating a feedback loop from Defense -> Persona -> Text.

_GLHF. Keep the empathy pixelated, but the code surgical._
