---
export const prerender = false

import DashboardLayout from '@/layouts/DashboardLayout.astro'
import { EvaluationPage } from '@/components/journal-research/pages'

// @ts-expect-error Astro.props is injected at runtime
const { user } = Astro.props as { user: any }

const sessionId = Astro.url.searchParams.get('sessionId')
---

<DashboardLayout
  title="Source Evaluation | Pixelated Empathy"
  description="Evaluate discovered journal data sources"
>
  <div class="container mx-auto py-6 space-y-6">
    <div class="flex items-center justify-between">
      <div>
        <h1 class="text-3xl font-bold">Source Evaluation</h1>
        <p class="text-muted-foreground mt-1">
          Evaluate and assess discovered sources for quality and relevance
        </p>
      </div>
    </div>

    <EvaluationPage client:load sessionId={sessionId} />
  </div>
</DashboardLayout>

